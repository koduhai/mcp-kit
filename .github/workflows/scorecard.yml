name: Scorecard

on:
  branch_protection_rule:
  schedule:
    - cron: '27 6 * * 2' # weekly, Tuesday 06:27 UTC
  push:
    branches: [main]

# Least-privilege at the top level; the job elevates only what it needs.
permissions: read-all

jobs:
  analysis:
    name: Scorecard analysis
    runs-on: ubuntu-latest
    permissions:
      security-events: write # upload SARIF results
      id-token: write # publish results to the OpenSSF API
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false
      - uses: ossf/scorecard-action@4eaacf0543bb3f2c246792bd56e8cdeffafb205a # v2.4.3
        with:
          results_file: results.sarif
          results_format: sarif
          publish_results: true
          # Read-only PAT so the Branch-Protection check can read protection
          # rules (the default GITHUB_TOKEN cannot), instead of erroring out.
          # Create a fine-grained PAT with Administration: read + Metadata: read
          # on this repo, save it as the SCORECARD_TOKEN secret. Optional: the
          # scan still runs without it, just skipping Branch-Protection.
          repo_token: ${{ secrets.SCORECARD_TOKEN }}
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: SARIF file
          path: results.sarif
          retention-days: 5
      - uses: github/codeql-action/upload-sarif@54f647b7e1bb85c95cddabcd46b0c578ec92bc1a # v4.36.3
        with:
          sarif_file: results.sarif
