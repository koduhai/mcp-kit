<!-- Thanks for contributing! Keep PRs small and focused on one logical change.
     PR title must follow Conventional Commits (e.g. "feat: ..."): it becomes the
     squash commit and the release note. CI enforces it. -->

## Summary

<!-- What does this change, and why? -->

## Related issues

<!-- e.g. Closes #123 -->

## Checklist

- [ ] PR title follows Conventional Commits (`<type>: <summary>`)
- [ ] `npm run lint` and `npm run format:check` pass
- [ ] `npm run typecheck` and `npm run build` pass
- [ ] `npm test` passes, and new behavior is covered by tests
- [ ] README / docs updated if the public API or behavior changed
- [ ] No secrets, tokens, or credentials in the diff
